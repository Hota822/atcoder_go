package main

import (
	"fmt"
	// "bufio"
	// "os"
	// "strings"
	// "strconv"
	// "reflect"
	// "math"
	// "sort"
)

// var sc = bufio.NewScanner(os.Stdin)

func main() {
	var x int 
	fmt.Scan(&x)
	// sc.Split(bufio.ScanWords)
	fmt.Println(x
}


func next() string {
	sc.Scan()
    ret := sc.Text()
    return ret
}

func nextInt() int {
    sc.Scan()
    ret, _ := strconv.Atoi(sc.Text())
    return ret
}